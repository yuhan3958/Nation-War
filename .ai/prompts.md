# Prompts for AI-assisted development

## New Module

Create a new Gradle module named `...` with the following structure:
- `...-common`
- `...-client`
- `...-server`
- `...-mod`

The module should have the following dependencies:
...

## New Feature

Implement a new feature `...` in the `...` module.

The feature should have the following requirements:
...
